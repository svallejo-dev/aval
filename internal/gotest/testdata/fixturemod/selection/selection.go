// Package selection is the code the selection fixtures exercise. It does
// nothing interesting: the point of the package is the shape of its test
// names.
package selection

// Level is the nesting level the tests start at.
func Level() int { return 0 }
